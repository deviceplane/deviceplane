version: 2
jobs:
  test:
    docker:
      - image: circleci/golang:1.12
    steps:
      - checkout
      - run: go test -v ./...

  release-controller:
    docker:
      - image: circleci/golang:1.12
    steps:
      - checkout
      - setup_remote_docker
      - run: curl -fsSL get.docker.com -o get-docker.sh && sh get-docker.sh
      - run: echo $DOCKER_PASSWORD | docker login -u $DOCKER_USER --password-stdin
      - run: make push-controller

  release-agent:
    docker:
      - image: circleci/golang:1.12
    steps:
      - checkout
      - setup_remote_docker
      - run: curl -fsSL get.docker.com -o get-docker.sh && sh get-docker.sh
      - run: echo $DOCKER_PASSWORD | docker login -u $DOCKER_USER --password-stdin
      - run: make push-agent

workflows:
  version: 2
  jobs:
    jobs:
      - test
      - release-controller:
          requires:
            - test
      - release-agent:
          requires:
            - test
